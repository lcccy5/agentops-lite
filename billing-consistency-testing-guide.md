# AgentOps Lite 计费一致性测试实施指引

## 1. 目的与范围

本指引用于指导开发与测试同事验证计费链路在请求重试、消息重复、进程崩溃和跨存储更新失败时的正确性，并产出可复现的测试数据。

需要回答两个问题：

1. 同一业务操作在重复触发或异常恢复后，是否发生重复扣费、漏扣费或预占未释放？
2. MySQL 账本与 Redis 用量投影出现差异后，系统能否自动恢复，恢复需要多久？

当前指引依据项目描述中的 Spring Boot、MySQL、Redis、Kafka、Server/Worker 架构编写，尚未核对仓库中的接口、表结构、部署配置和计费状态机。文中的目录、测试类、配置名均为建议，实施时应映射到实际代码。

本轮默认验证 MySQL 与 Redis 的内部一致性。第三方账单对账如需纳入，应单独准备可核验账单与场景，不得用内部投影测试替代第三方对账验证。

## 2. 代码组织：集中管理，按职责分目录

建议将 Java 测试集中放在一个计费一致性测试包下，并按职责拆分子包；不要把全部测试写入一个类。部署文件、负载脚本和报告不放入 Java 包。

如果 Server 和 Worker 分属多个模块，优先建立独立的集成测试模块，负责启动或连接两个服务；如果当前是单模块项目，先使用现有模块的 src/test 即可，不必为了测试立即拆模块。

建议结构（基础包名替换为项目实际值）：

```text
src/test/java/<base-package>/billing/consistency/
  scenario/       正常结算、重复消息、崩溃恢复、补偿竞争等测试
  fixture/        租户、账户、费率、请求和固定用量数据
  fault/          故障注入控制与故障触发确认
  assertion/      账本、余额、预占与投影一致性断言
  reporting/      样本导出与运行摘要
src/test/resources/billing-consistency/
  application-test.yml
  fixtures/
tests/billing-consistency/
  compose.yml     独立测试环境
  k6/             正常流量与幂等重试脚本
  scripts/        启动、就绪检查、数据校验与报告汇总
  README.md       实际运行命令与环境要求
test-results/billing-consistency/<run-id>/
  manifest.json
  operations.csv
  repairs.csv
  summary.json
```

端到端测试不得仅用测试进程里的 Mock 替换远程 Worker 的行为。跨进程故障需要通过测试环境中的服务控制机制实现。

## 3. 实施前必须确认的业务约定

由开发同事先补齐以下内容，再编写断言：

| 约定 | 必须明确的内容 |
|---|---|
| 业务操作唯一标识 | requestId、operationId 或其他实际字段；与 HTTP 请求次数、Kafka 投递次数的关系 |
| 幂等范围 | 哪些接口支持相同幂等键重试；不同租户之间如何隔离 |
| 金额计算 | 费率、Token 用量来源、金额精度、舍入规则；使用整数最小单位或精确十进制 |
| 状态机 | 预占、结算、补偿的合法迁移；迟到结算与已补偿状态如何处理 |
| 事实来源 | 哪些 MySQL 数据为权威事实，哪些 Redis 数据属于可重建投影 |
| 自动恢复方式 | 消息重试、补偿任务、周期对账的触发条件和间隔 |
| 通过标准 | 允许的恢复时限、测试观察窗口、预期终态和余额规则 |

预期金额应由固定输入和明确费率独立计算，不要直接调用被测结算逻辑生成预期值，否则可能同时复制实现中的错误。

## 4. 工具与职责

| 工具 | 用途 | 本轮要求 |
|---|---|---|
| Docker Compose | 部署独立 MySQL、Redis、Kafka、Server、Worker 和模型桩 | 推荐作为首轮统一环境 |
| JUnit 5 + Spring Boot Test | 编排场景、准备数据、驱动接口与验证业务状态 | 必需 |
| Awaitility | 等待异步终态与恢复条件成立 | 推荐；轮询耗时不能直接当作精确修复耗时 |
| k6 | 制造并发流量、独立请求与同幂等键重试 | 在确定性场景通过后加入 |
| SQL + Java/Python 汇总脚本 | 校验账务与生成样本级报告、P95 | 必需，可任选一种汇总语言 |
| 固定用量模型桩 | 返回确定的 usage 和成功/失败响应，使预期费用可计算 | 推荐；无需使用真实模型计费 |
| Testcontainers | 自动启动一次性依赖，便于以后接入 CI | 可选，不必与 Compose 重复建设 |
| Toxiproxy | 注入依赖网络延迟、断连 | 可选，适合后续网络故障场景 |
| Prometheus/Grafana | 观察消费积压、错误与恢复趋势 | 可选，不替代账务校验和原始明细 |

工具资料：

- k6 HTTP 请求：https://grafana.com/docs/k6/latest/using-k6/http-requests/
- Awaitility 使用与轮询限制：https://github.com/awaitility/awaitility/wiki/Usage
- Testcontainers Toxiproxy：https://java.testcontainers.org/modules/toxiproxy/

## 5. 最小测试场景

| ID | 场景与操作 | 解决的问题 | 核心断言 |
|---|---|---|---|
| C01 | 正常请求完成预占与结算 | 建立基础正确性基线 | 预期金额等于实际净扣费；状态与预占符合约定 |
| C02 | 同一业务幂等键顺序重试与并发重试 | 客户端重试是否造成重复收费 | 在接口幂等约定范围内只产生一次业务收费 |
| C03 | 重复投递同一结算事件 | Kafka 重复处理是否影响账务或投影 | 有效结算与净扣费正确，Redis 不重复累计 |
| C04 | 业务事务提交后、消费位点提交前终止 Worker，再重启 | 崩溃后的真实重新投递是否安全 | 确认发生重投递；最终金额与投影正确 |
| C05 | MySQL 已提交，Redis 投影更新失败 | 跨存储部分成功能否自动修复 | 确认差异存在；自动恢复一致并记录耗时 |
| C06 | 预占后未完成结算，等待真实超时补偿 | 悬挂预占能否回收 | 按状态机完成补偿，无残留预占或错误退款 |
| C07 | 使用同步屏障控制结算与补偿竞争 | 竞争条件是否造成双重处理 | 最终迁移合法，扣费与释放不重复 |

以上故障点必须根据真实事务边界、Kafka 位点提交方式和投影更新路径选择。若项目不存在对应路径，应调整场景并说明原因。

消息重复测试应保留同一业务事件标识；新建事件 ID 的消息可能代表不同业务操作，不能直接视作幂等验证。

## 6. 故障如何注入

优先使用可定位到业务阶段、可证明已经触发的测试专用注入点，例如：

```text
after_ledger_commit_before_projection
after_business_commit_before_offset_commit
before_settlement_transition
before_compensation_transition
```

每个注入点至少关联 run_id、operation_id、故障类型及触发次数。测试必须等到目标操作到达注入点后再执行中断或放行，不要只靠固定 sleep 猜测时序。

要求：

1. 注入能力仅在隔离测试配置启用，默认关闭，不向正常业务环境暴露故障控制入口。
2. 支持单次触发或明确次数，避免持续故障导致后续恢复永远无法完成。
3. 记录实际触发证据；未触发的样本标为无效场景，不得算作测试通过。
4. 崩溃场景应使用非优雅终止模拟进程突然退出；正常关闭可能先完成清理，测不到目标窗口。
5. 区分应用异常、进程崩溃与网络故障；在 MySQL 提交前抛异常导致事务回滚，不能声称已覆盖提交后的跨存储差异。

## 7. 指标与统计口径

### 7.1 账务正确性

以唯一业务操作为核验单位，HTTP 尝试次数和 Kafka 投递次数单独统计。

| 指标 | 口径 |
|---|---|
| 唯一业务操作数 | 本次运行不同业务操作 ID 的数量 |
| 请求尝试数 | 包含同一业务操作重试的请求总次数 |
| 重复扣费操作数 | 根据账本分录及状态迁移，确认同一收费义务发生重复扣款的操作数 |
| 漏扣或金额不符操作数 | 按业务约定应扣金额与实际净扣费不符的操作数 |
| 预占残留数 | 应进入终态且应释放预占，但观察截止时仍残留的操作数 |
| 未完成操作数 | 观察窗口结束后仍未进入预期终态的操作数 |

不能仅统计数据库记录数量：一笔业务可能合法生成预占、结算、释放等多条分录。也不能只核对最终净额：重复扣款后被退款抵消，仍应识别为发生过重复扣款。

同时进行账户级校验：测试账户的期初余额与全部账务变动应能解释期末余额，并按实际设计分别核对可用余额、预占余额与累计消费。使用独立测试账户，排除外部流量干扰。

### 7.2 差异修复耗时

本轮默认差异指 MySQL 权威账本与 Redis 对应用量投影不一致。

```text
repair_duration_ms = 一致性恢复时间 t1 - 差异实际产生时间 t0
```

- t0 应关联造成差异的已提交业务事实，不能从对账任务发现差异时才开始计时。
- t1 应有投影修复完成的事件及一致性核验支撑；日志显示“开始修复”不等于恢复完成。
- 耗时包含消息等待、重试退避、补偿或对账调度等待。
- 多进程时间戳需要同步时钟，并记录精度；由测试进程轮询确认恢复时，应记录轮询间隔，并将结果说明为观测耗时。
- 正常请求不进入差异修复耗时样本集。
- 故障持续时间、恢复机制和故障类型应记录并分别汇总，避免混合不同测试配置后给出无法解释的 P95。

必须同时输出：有效差异样本数、已修复数、超时未修复数，以及已修复样本的 P50、P95、最大耗时。

P95 统一采用最近秩法：将 n 个已修复样本耗时升序排列，取第 ceil(0.95 × n) 个值。存在超时未修复样本时，必须标明这是“已修复样本 P95”，同时展示超时数量，不能将其描述为全部差异的恢复保证。

开始前约定恢复时限与观察窗口。故意延长故障会影响结果，应固定故障持续时间并写入报告。

## 8. 如何部署与执行

### 阶段 A：准备独立环境

1. 使用独立数据库、Redis 命名空间、Kafka topic/consumer group 和测试账户，避免混入其他运行的数据。
2. 通过 Compose 启动 MySQL、Redis、Kafka、模型桩、Server、Worker；服务地址按实际配置注入。
3. Server 与 Worker 使用相同待测代码版本，执行项目真实数据库迁移。
4. 固定费率、模型桩 usage、重试策略、Worker 并发、补偿及对账周期。
5. 检查数据库、Kafka、Redis 和服务就绪后再开始测试，不能仅检查容器已启动。
6. 为本次运行分配 run_id，保存提交版本、环境资源和所有影响结果的配置。

运行入口建议统一为以下顺序，由实施同事提供可复制的实际命令：

```text
启动依赖与应用 → 等待就绪 → 初始化数据 → 执行场景
→ 等待恢复与消费排空 → 核验账务 → 导出报告 → 清理本次资源
```

本指引不提供尚未核验的 Maven 模块名、端口或一键命令。实施交付时必须在 tests/billing-consistency/README.md 中补齐。

### 阶段 B：先验证确定性场景

1. 先执行 C01，确认模型桩、费率与账务核验正确。
2. 每个故障场景先使用少量操作，验证实际触发目标故障。
3. 对断言做一次负向验证：例如在隔离的校验测试夹具中放入已知金额偏差，确认核验器确实报错。
4. 验证系统通过自身机制恢复；手动修数据库不能计为自动修复成功。

### 阶段 C：重复采样与并发验证

1. 重复运行差异场景，建议初轮每类积累至少 100 个有效差异样本，作为探索性统计；这不代表已证明低概率故障不存在。
2. 之后加入 k6，按机器容量逐档增加并发，每档固定运行时长或操作数量。
3. 正常请求、重复请求和故障样本分别计数；并发操作数与并发连接数不要混用。
4. 流量停止后继续观察异步链路，直至完成或达到事先约定的截止时间，再执行最终账务核验。
5. 不同并发、故障持续时间或恢复配置分别报告，不只挑选最好的一次。

### 阶段 D：归档与清理

先导出样本和日志，再清理本次 run_id 对应的测试数据或专用环境。清理脚本必须限定测试资源范围。运行产物建议忽略版本控制，脚本、配置模板和脱敏报告样例纳入版本控制。

## 9. 必须交付的文件

| 文件 | 内容 |
|---|---|
| manifest.json | run_id、代码版本、镜像版本、CPU/内存、并发、费率、固定 usage、重试及调度配置、计时方式 |
| operations.csv | 操作 ID、场景、请求尝试数、预期金额、实际净扣费、有效结算次数、预占、终态、核验结果 |
| repairs.csv | 差异 ID、操作 ID、故障类型、触发证据、t0、t1、耗时、是否超时 |
| summary.json | 各场景有效样本数、无效样本数、账务异常数、修复数、未修复数、耗时分位数 |
| README.md | 环境要求、真实执行命令、场景说明、指标定义、已知限制和复现步骤 |

建议摘要模板：

```text
代码版本：
测试环境与并发：
模型服务：固定 usage 模型桩
唯一业务操作数 / 请求尝试数：
已覆盖故障及每类有效样本数：
重复扣费 / 漏扣或金额不符 / 预占残留 / 未完成操作数：
差异样本数 / 已修复数 / 超时未修复数：
各故障场景已修复样本 P50 / P95 / 最大耗时：
观察窗口与计时误差：
报告位置与复现命令：
```

## 10. 完成标准与对外表述

完成标准：所有约定场景可复现，故障触发有证据，账務断言能识别错误，报告保留样本明细；正确性场景无重复扣费、漏扣或其他违背业务约定的结果，恢复时效符合预先约定的目标。失败必须在报告中明确列出，不能通过删除样本使结果达标。

简历结果只使用实际测得的数据，例如：

> 在【N】个业务操作及重复消息、Worker 崩溃等故障注入测试中，未观察到重复扣费；【M】个 MySQL—Redis 用量投影差异样本全部在观察窗口内自动修复，修复耗时 P95 为【T】。

若存在未修复样本，应如实披露，或先修复问题并重新运行测试。使用模型桩时，在报告中注明这是计费治理链路测试，不代表真实模型服务性能或真实第三方账单对账效果。
